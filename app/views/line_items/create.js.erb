$('#notice').hide();

if ($('#cart tr').length > 1) { $('#cart').show('slideDown', 1000); }

$('#cart').html("<%= escape_javascript render(@cart) %>");
$('.collapsible').collapsible({ accordion : false });
